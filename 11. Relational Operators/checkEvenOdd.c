#include <stdio.h> 

int main(void) { 
    
    int num = 20;

    if((num % 2) == 0) {

      printf("%d is a Even number\n", num);

    } else {

      printf("%d is a Odd number\n", num);

    }
    
  return 0;
}
